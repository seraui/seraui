export const metadata = {
    title: 'Moving Grid Component - Sera UI Component Library',
    description: 'Animated moving grid background component with customizable patterns and motion effects. Perfect for dynamic backgrounds and visual enhancements.',
    keywords: [
        'React moving grid',
        'animated grid background',
        'moving pattern',
        'dynamic grid',
        'animated background',
        'grid animation',
        'Tailwind CSS moving grid',
        'UI animated background',
        'motion graphics',
        'Sera UI components'
    ],
    openGraph: {
        title: 'Moving Grid Component - Sera UI',
        description: 'Animated React moving grid background component with customizable patterns and motion effects for dynamic backgrounds.',
        type: 'article',
        url: '/docs/moving-grid',
    },
    twitter: {
        card: 'summary_large_image',
        title: 'Moving Grid Component - Sera UI',
        description: 'Animated React moving grid background component with customizable patterns and motion effects for dynamic backgrounds.',
    },
    alternates: {
        canonical: '/docs/moving-grid',
    },
};

import { CodeBlock } from "@/components/site/code-block";
import { ComponentRenderer } from "@/components/site/component-renderer";
import { Cli } from "@/components/site/cli";
import {
  Tabs,
  TabsList,
  TabsTrigger,
  TabsContent,
} from "@/components/site/tabs.tsx";
import { HideTOC } from "@/components/site/hide-toc";
import GridBackgroundView from "./moving-grid"

<HideTOC />

<AI dir="left"/>

# Smooth Moving Grid
A dynamic background with mouse-aware parallax effect.



<Tabs defaultValue="preview" className="">
  <TabsList>
    <TabsTrigger value="preview">Preview</TabsTrigger>
    <TabsTrigger value="code">Code</TabsTrigger>
  </TabsList>

  {/* Preview tab renders the interactive CopyButtonView component */}
  <TabsContent value="preview">
    <ComponentRenderer component={<GridBackgroundView />} />
  </TabsContent>

  {/* Code tab displays the source code of the CopyButtonView component */}
  <TabsContent value="code">
    <CodeBlock filePath="src/app/docs/moving-grid/moving-grid.tsx" />
  </TabsContent>
</Tabs>
