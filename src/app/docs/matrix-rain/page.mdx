import { CodeBlock } from "@/components/site/code-block";
import { ComponentRenderer } from "@/components/site/component-renderer";
import { Cli } from "@/components/site/cli";
import {
    Tabs,
    TabsList,
    TabsTrigger,
    TabsContent,
} from "@/components/site/tabs.tsx";
import MatrixRainView from "./matrix-rain"

#  Matrix Rain
Matrix is a React component that renders a matrix-like effect on the screen. It is a fun and interactive way to display text in a visually appealing way.


<Tabs defaultValue="preview" className="">
  <TabsList>
    <TabsTrigger value="preview">Preview</TabsTrigger>
    <TabsTrigger value="code">Code</TabsTrigger>
  </TabsList>

  {/* Preview tab renders the interactive CopyButtonView component */}
  <TabsContent value="preview">
    <ComponentRenderer component={<MatrixRainView />} />
  </TabsContent>

  {/* Code tab displays the source code of the CopyButtonView component */}
  <TabsContent value="code">
    <CodeBlock filePath="src/app/docs/matrix-rain/matrix-rain.tsx" />
  </TabsContent>
</Tabs>
