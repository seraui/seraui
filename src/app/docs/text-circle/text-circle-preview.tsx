import { TextCircle } from "@/components/core/text-circle";

const TextCirclePreview = () => {
  return (
    <TextCircle
      text="BERLIX UI * BERLIX UI * "
      className="font-black text-3xl"
    />
  );
};

export default TextCirclePreview;
