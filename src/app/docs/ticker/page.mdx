export const metadata = {
    title: 'React Ticker Component | Sera UI',
    description: 'Animated ticker component with scrolling text, customizable speed, and direction controls. Perfect for news feeds, announcements, and continuous content display.',
    keywords: [
        'React ticker component',
        'scrolling ticker',
        'news ticker',
        'animated ticker',
        'text ticker',
        'ticker tape',
        'Tailwind CSS ticker',
        'UI ticker library',
        'scrolling text',
        'Sera UI components'
    ],
    openGraph: {
        title: 'React Ticker Component | Sera UI',
        description: 'Animated React ticker component with scrolling text, customizable speed, and direction controls for news feeds and announcements.',
        type: 'article',
        url: '/docs/ticker',
        images: [
          {
            url: "/og-image.png",
            width: 1200,
            height: 630,
            alt: "Sera UI - Modern React Component Library",
          },
        ],
    },
    twitter: {
        card: 'summary_large_image',
        title: 'Ticker Component | Sera UI',
        description: 'Animated React ticker component with scrolling text, customizable speed, and direction controls for news feeds and announcements.',
        images: ["/og-image.png"]
    },
    alternates: {
        canonical: '/docs/ticker',
    },
};

import { CodeBlock } from "@/components/site/code-block";
import { ComponentRenderer } from "@/components/site/component-renderer";
import { Cli } from "@/components/site/cli";
import {
  Tabs,
  TabsList,
  TabsTrigger,
  TabsContent,
} from "@/components/site/tabs.tsx";
import Tickerview from "./ticker-view"
import NumberTicker from "./ticker"


# Number Ticker <AI dir="left"/>
A smooth animated number counter component that animates from 0 to any target value with customizable formatting and styling.

<Tabs defaultValue="preview" className="">
  <TabsList>
    <TabsTrigger value="preview">Preview</TabsTrigger>
    <TabsTrigger value="code">Code</TabsTrigger>
  </TabsList>
  <TabsContent value="preview">
    <ComponentRenderer component={<Tickerview />} reTrigger={true}/>
  </TabsContent>
  <TabsContent value="code">
    <CodeBlock filePath="src/app/docs/ticker/ticker.tsx" />
  </TabsContent>
</Tabs>


## Usage

```tsx
import NumberTicker from "./ticker"

export default function Example() {
  return (
    <NumberTicker
      value={100}
      duration={2500}
      className="text-4xl font-bold"
      prefix="$"
      decimalPlaces={2}
    />
  )
}
```

## Props

<div className="my-8 overflow-x-auto">
  <table className="w-full text-sm border border-gray-200 dark:border-gray-700">
    <thead className="bg-gray-50 dark:bg-gray-800 text-left font-semibold text-gray-700 dark:text-gray-200 border-b border-gray-200 dark:border-gray-700">
      <tr>
        <th className="px-4 py-2 border-r border-gray-200 dark:border-gray-700">Prop</th>
        <th className="px-4 py-2 border-r border-gray-200 dark:border-gray-700">Type</th>
        <th className="px-4 py-2 border-r border-gray-200 dark:border-gray-700">Default</th>
        <th className="px-4 py-2">Description</th>
      </tr>
    </thead>
    <tbody className="text-gray-800 dark:text-gray-100">
      <tr className="even:bg-gray-50 dark:even:bg-gray-800/40 border-b border-gray-200 dark:border-gray-700">
        <td className="px-4 py-2 border-r border-gray-200 dark:border-gray-700"><code>value</code></td>
        <td className="px-4 py-2 border-r border-gray-200 dark:border-gray-700"><code>number</code></td>
        <td className="px-4 py-2 border-r border-gray-200 dark:border-gray-700">–</td>
        <td className="px-4 py-2">The target number to animate to</td>
      </tr>
      <tr className="even:bg-gray-50 dark:even:bg-gray-800/40 border-b border-gray-200 dark:border-gray-700">
        <td className="px-4 py-2 border-r border-gray-200 dark:border-gray-700"><code>duration</code></td>
        <td className="px-4 py-2 border-r border-gray-200 dark:border-gray-700"><code>number</code></td>
        <td className="px-4 py-2 border-r border-gray-200 dark:border-gray-700"><code>2000</code></td>
        <td className="px-4 py-2">Animation duration in milliseconds</td>
      </tr>
      <tr className="even:bg-gray-50 dark:even:bg-gray-800/40 border-b border-gray-200 dark:border-gray-700">
        <td className="px-4 py-2 border-r border-gray-200 dark:border-gray-700"><code>delay</code></td>
        <td className="px-4 py-2 border-r border-gray-200 dark:border-gray-700"><code>number</code></td>
        <td className="px-4 py-2 border-r border-gray-200 dark:border-gray-700"><code>0</code></td>
        <td className="px-4 py-2">Delay before animation starts in milliseconds</td>
      </tr>
      <tr className="even:bg-gray-50 dark:even:bg-gray-800/40 border-b border-gray-200 dark:border-gray-700">
        <td className="px-4 py-2 border-r border-gray-200 dark:border-gray-700"><code>decimalPlaces</code></td>
        <td className="px-4 py-2 border-r border-gray-200 dark:border-gray-700"><code>number</code></td>
        <td className="px-4 py-2 border-r border-gray-200 dark:border-gray-700"><code>0</code></td>
        <td className="px-4 py-2">Number of decimal places to display</td>
      </tr>
      <tr className="even:bg-gray-50 dark:even:bg-gray-800/40 border-b border-gray-200 dark:border-gray-700">
        <td className="px-4 py-2 border-r border-gray-200 dark:border-gray-700"><code>prefix</code></td>
        <td className="px-4 py-2 border-r border-gray-200 dark:border-gray-700"><code>string</code></td>
        <td className="px-4 py-2 border-r border-gray-200 dark:border-gray-700"><code>""</code></td>
        <td className="px-4 py-2">Text to display before the number (e.g. `$`, `€`)</td>
      </tr>
      <tr className="even:bg-gray-50 dark:even:bg-gray-800/40 border-b border-gray-200 dark:border-gray-700">
        <td className="px-4 py-2 border-r border-gray-200 dark:border-gray-700"><code>suffix</code></td>
        <td className="px-4 py-2 border-r border-gray-200 dark:border-gray-700"><code>string</code></td>
        <td className="px-4 py-2 border-r border-gray-200 dark:border-gray-700"><code>""</code></td>
        <td className="px-4 py-2">Text to display after the number (e.g. `%`, `+`, `K`)</td>
      </tr>
      <tr className="even:bg-gray-50 dark:even:bg-gray-800/40 border-b border-gray-200 dark:border-gray-700">
        <td className="px-4 py-2 border-r border-gray-200 dark:border-gray-700"><code>className</code></td>
        <td className="px-4 py-2 border-r border-gray-200 dark:border-gray-700"><code>string</code></td>
        <td className="px-4 py-2 border-r border-gray-200 dark:border-gray-700"><code>""</code></td>
        <td className="px-4 py-2">Additional CSS classes for styling</td>
      </tr>
      <tr className="even:bg-gray-50 dark:even:bg-gray-800/40">
        <td className="px-4 py-2 border-r border-gray-200 dark:border-gray-700"><code>onComplete</code></td>
        <td className="px-4 py-2 border-r border-gray-200 dark:border-gray-700"><code>function</code></td>
        <td className="px-4 py-2 border-r border-gray-200 dark:border-gray-700">–</td>
        <td className="px-4 py-2">Callback function called when animation completes</td>
      </tr>
    </tbody>
  </table>
</div>

## Examples

### Basic Counter
```tsx
<NumberTicker value={42} />
```

### Currency Display
```tsx
<NumberTicker
  value={1234.56}
  prefix="$"
  decimalPlaces={2}
  className="text-2xl font-bold text-green-600"
/>
```

### Percentage with Callback
```tsx
<NumberTicker
  value={95.5}
  suffix="%"
  decimalPlaces={1}
  duration={3000}
  onComplete={() => console.log('Animation complete!')}
/>
```
