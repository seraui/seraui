export const metadata = {
    title: 'Glow Line Component - Sera UI Component Library',
    description: 'Animated glow line component with customizable colors, directions, and pulsing effects. Perfect for dividers, borders, and decorative elements.',
    keywords: [
        'React glow line',
        'animated line',
        'glowing divider',
        'line with glow effect',
        'animated border',
        'pulsing line',
        'Tailwind CSS glow line',
        'UI glow effects',
        'decorative line',
        'Sera UI components'
    ],
    openGraph: {
        title: 'Glow Line Component - Sera UI',
        description: 'Animated React glow line component with customizable colors, directions, and pulsing effects for decorative elements.',
        type: 'article',
        url: '/docs/glowline',
    },
    twitter: {
        card: 'summary_large_image',
        title: 'Glow Line Component - Sera UI',
        description: 'Animated React glow line component with customizable colors, directions, and pulsing effects for decorative elements.',
    },
    alternates: {
        canonical: '/docs/glowline',
    },
};

import { CodeBlock } from "@/components/site/code-block";
import { ComponentRenderer } from "@/components/site/component-renderer";
import { Cli } from "@/components/site/cli";
import {
    Tabs,
    TabsList,
    TabsTrigger,
    TabsContent,
} from "@/components/site/tabs.tsx";
import { MdxTable } from "@/components/site/mdx-table.tsx"
import GlowlineView from "./glowline-view"



# GlowLine <AI dir="left"/>

A customizable glowing line component that creates beautiful neon-like effects. Perfect for adding visual flair to your UI with horizontal or vertical glowing lines in various colors.

## Demo

<Tabs defaultValue="preview" className="">
  <TabsList>
    <TabsTrigger value="preview">Preview</TabsTrigger>
    <TabsTrigger value="code">Code</TabsTrigger>
  </TabsList>
  <TabsContent value="preview">
    <ComponentRenderer component={<GlowlineView />} />
  </TabsContent>
  <TabsContent value="code">
    <CodeBlock filePath="src/app/docs/glowline/glowline.tsx" />
  </TabsContent>
</Tabs>

## Props

<MdxTable>
| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `orientation` | `'vertical' \| 'horizontal'` | - | Sets the direction of the glow line |
| `position` | `string` | - | CSS position value (e.g., '50%', '100px') |
| `color` | `'purple' \| 'blue' \| 'green' \| 'red'` | - | Color scheme for the glow effect |
| `className` | `string` | `''` | Additional CSS classes for custom styling |
</MdxTable>

## Usage Examples

### Basic Usage

```tsx
import { GlowLine } from './components/GlowLine';

export default function Example() {
  return (
    <div className="relative w-full h-screen bg-gray-900">
      <GlowLine 
        orientation="horizontal" 
        position="50%" 
        color="blue" 
      />
    </div>
  );
}