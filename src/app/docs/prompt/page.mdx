export const metadata = {
    title: 'React Prompt Component - React and Next.js UI Components | Sera UI',
    description: 'Interactive prompt and command input components with autocomplete, syntax highlighting, and terminal-like styling. Perfect for CLI interfaces and developer tools.',
    keywords: [
        'React prompt component',
        'command prompt',
        'terminal component',
        'CLI interface',
        'command input',
        'developer tools',
        'Tailwind CSS prompt',
        'UI terminal library',
        'command line interface',
        'Sera UI components'
    ],
    openGraph: {
        title: 'Prompt Component - Sera UI',
        description: 'Interactive React prompt and command input components with autocomplete and terminal-like styling for CLI interfaces.',
        type: 'article',
        url: '/docs/prompt',
    },
    twitter: {
        card: 'summary_large_image',
        title: 'Prompt Component - Sera UI',
        description: 'Interactive React prompt and command input components with autocomplete and terminal-like styling for CLI interfaces.',
    },
    alternates: {
        canonical: '/docs/prompt',
    },
};

import { CodeBlock } from "@/components/site/code-block";
import { ComponentRenderer } from "@/components/site/component-renderer";
import { Cli } from "@/components/site/cli";
import {
  Tabs,
  TabsList,
  TabsTrigger,
  TabsContent,
} from "@/components/site/tabs.tsx";
import Prompt from "./prompt.tsx"
import VercelV0Chat from "./v0.tsx"
import Prompting from "./prompt2.tsx"



# Chatbot UI <AI dir="left"/>
A chatbot UI component with a sleek design and modern features. Features a clean and minimalistic design with a modern glassmorphism effect, dark mode compatibility, and a seamless transition between messages. Perfect for creating engaging and interactive user experiences.



<Tabs defaultValue="preview" className="">
  <TabsList>
    <TabsTrigger value="preview">Preview</TabsTrigger>
    <TabsTrigger value="code">Code</TabsTrigger>
  </TabsList>
  <TabsContent value="preview">
    <ComponentRenderer component={<Prompting />} />
  </TabsContent>
  <TabsContent value="code">
    <CodeBlock filePath="src/app/docs/prompt/prompt.tsx" />
  </TabsContent>
</Tabs>


# Vercel v0 Chat

<Tabs defaultValue="preview" className="">
  <TabsList>
    <TabsTrigger value="preview">Preview</TabsTrigger>
    <TabsTrigger value="code">Code</TabsTrigger>
  </TabsList>
  <TabsContent value="preview">
    <ComponentRenderer component={<VercelV0Chat />} />
  </TabsContent>
  <TabsContent value="code">
    <CodeBlock filePath="src/app/docs/prompt/prompt.tsx" />
  </TabsContent>
</Tabs>


## Basic Prompt Input
A basic prompt input component with model selection and file attachment.

<Tabs defaultValue="preview" className="">
  <TabsList>
    <TabsTrigger value="preview">Preview</TabsTrigger>
    <TabsTrigger value="code">Code</TabsTrigger>
  </TabsList>
  <TabsContent value="preview">
    <ComponentRenderer component={<Prompt />} />
  </TabsContent>
  <TabsContent value="code">
    <CodeBlock filePath="src/app/docs/prompt/prompt.tsx" />
  </TabsContent>
</Tabs>



## Basic Usage

After installation, import and use the component in your React application:

```tsx
import PromptComponent from './prompt';

function App() {
  return (
    <div className="min-h-screen bg-gray-50 dark:bg-gray-900 p-8">
      <PromptComponent />
    </div>
  );
}
```

