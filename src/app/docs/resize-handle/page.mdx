export const metadata = {
    title: 'Resize Handle Component - Sera UI Component Library',
    description: 'Interactive resize handle component for creating resizable panels, dividers, and layout controls. Perfect for dashboard layouts and adjustable interfaces.',
    keywords: [
        'React resize handle',
        'resizable component',
        'resize divider',
        'panel resize',
        'layout control',
        'resizable panels',
        'Tailwind CSS resize',
        'UI resize library',
        'interactive resize',
        'Sera UI components'
    ],
    openGraph: {
        title: 'Resize Handle Component - Sera UI',
        description: 'Interactive React resize handle component for creating resizable panels, dividers, and layout controls.',
        type: 'article',
        url: '/docs/resize-handle',
    },
    twitter: {
        card: 'summary_large_image',
        title: 'Resize Handle Component - Sera UI',
        description: 'Interactive React resize handle component for creating resizable panels, dividers, and layout controls.',
    },
    alternates: {
        canonical: '/docs/resize-handle',
    },
};

import { CodeBlock } from "@/components/site/code-block";
import { ComponentRenderer } from "@/components/site/component-renderer";
import { Cli } from "@/components/site/cli";
import {
    Tabs,
    TabsList,
    TabsTrigger,
    TabsContent,
} from "@/components/site/tabs.tsx";
import { PropsTable } from "@/components/site/props-table.tsx";
import ResizeHandle from "./resize-handle";

<AI dir="left"/>

# Resize Handle Text

The **Resize Handle Text** component provides an animated, draggable resize handle with a flipping text effect and a stylish blurry transition. It's perfect for interactive layouts where you want to add a dynamic UI element.

It displays an array of vibrant words that flip as the user interacts with the handle—creating a polished, modern feel for your app or website.

<Tabs defaultValue="preview" className="mt-4">
  <TabsList>
    <TabsTrigger value="preview">Preview</TabsTrigger>
    <TabsTrigger value="code">Code</TabsTrigger>
  </TabsList>
  <TabsContent value="preview">
    <ComponentRenderer component={<ResizeHandle />} reTrigger={true} />
  </TabsContent>
  <TabsContent value="code">
    <CodeBlock filePath="src/app/docs/resize-handle/resize-handle.tsx" />
  </TabsContent>
</Tabs>

---

## Customizing Your Flipping Words

You can easily replace the text being flipped with your own words by updating the `phrases` array:

```tsx
const phrases = [
  "AWESOME",
  "BEAUTIFUL",
  "STUNNING",
  "COOL",
  "ELEGANT",
  "AMAZING",
  "VIBRANT",
  "DYNAMIC",
];
